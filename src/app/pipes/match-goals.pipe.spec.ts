import { MatchGoalsPipe } from './match-goals.pipe';

describe('MatchGoalsPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchGoalsPipe();
    expect(pipe).toBeTruthy();
  });
});
